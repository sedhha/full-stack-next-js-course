import classes from './RadioButton.module.css';

const RadioButton = () => {
	return (
		<div className={classes.OuterEnclosure}>
			<section className={classes.ToggleButton} />
		</div>
	);
};

export default RadioButton;
